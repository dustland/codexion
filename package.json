{
  "name": "codexion",
  "version": "0.3.2",
  "description": "A lightweight local companion for Codex Desktop.",
  "license": "MIT",
  "author": "LYU.ai",
  "homepage": "https://github.com/lyuai/codexion#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lyuai/codexion.git"
  },
  "bugs": {
    "url": "https://github.com/lyuai/codexion/issues"
  },
  "keywords": [
    "codex",
    "desktop",
    "cdp",
    "macos",
    "usage"
  ],
  "type": "module",
  "bin": {
    "codexion": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "pnpm build && node dist/index.js start",
    "attach": "pnpm build && node dist/index.js attach",
    "doctor": "pnpm build && node dist/index.js doctor",
    "package:mac": "bash scripts/build-macos-app.sh && bash scripts/create-dmg.sh",
    "format": "biome format --write src test package.json tsconfig.json biome.json vitest.config.ts",
    "lint": "biome check src test package.json tsconfig.json biome.json vitest.config.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "@types/node": "^24.0.0",
    "esbuild": "^0.28.2",
    "postject": "1.0.0-alpha.6",
    "typescript": "^5.9.0",
    "vitest": "^4.1.10"
  }
}
